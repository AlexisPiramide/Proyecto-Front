const URL = "http://localhost:8080/";

// validationConstants.js
export const MIN_NAME_LENGTH = 3;  // Minimum length for name fields
export const ID_PATTERN = /^[A-Za-z0-9]{4}-[A-Za-z0-9]{4}-[A-Za-z0-9]{4}$/;  // ID pattern
export const POSTAL_CODE_LENGTH = 5;  // Length for postal code
export const MIN_ADDRESS_NUMBER_LENGTH = 1;  // Minimum length for address number

export default URL;